async def async_client():
    """Global AsyncClient fixture for all test functions."""
    async with AsyncClient(app=app, base_url="http://test") as client:
        yield client  # Provides the client instance
    # Client automatically closes after all tests
